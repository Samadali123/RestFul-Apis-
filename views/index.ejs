<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Idea App</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    /* Hide scrollbar for cards container */
    .no-scrollbar::-webkit-scrollbar {
      display: none;
    }
    .no-scrollbar {
      -ms-overflow-style: none; /* Internet Explorer 10+ */
      scrollbar-width: none; /* Firefox */
    }
  </style>
</head>
<body class="bg-gray-50 text-gray-800 flex flex-col min-h-screen">
  <!-- Header -->
  <header class="bg-gradient-to-r from-blue-500 to-blue-700 text-white py-6 shadow-md">
    <h1 class="text-center text-4xl font-extrabold">Idea App</h1>
    <p class="text-center text-base font-medium mt-2">"Unleash Your Creativity, One Idea at a Time"</p>
  </header>

  <!-- Main Content -->
  <main class="flex-grow flex flex-col items-center my-10 px-4">
    <!-- Title and Create Button -->
    <div class="w-full max-w-4xl text-center mb-8">
      <h2 class="text-3xl font-semibold text-gray-800 mb-4">All Ideas</h2>
      <a href="/ideaApp/v1/ideas/create" class="bg-blue-600 text-white px-6 py-2 rounded-lg shadow hover:bg-blue-700 transition duration-200 inline-block">
        Create New Idea
      </a>
    </div>

    <!-- Ideas Grid -->
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6 w-full max-w-6xl h-96 overflow-auto no-scrollbar">
      <% ideas.forEach(idea => { %>
        <div class="bg-white rounded-lg shadow-md p-6 transition hover:shadow-lg hover:scale-105">
          <h3 class="text-xl font-bold text-gray-900"><%= idea.idea_name %></h3>
          <p class="text-sm text-gray-600 mt-1">By: <span class="font-medium text-blue-600"><%= idea.author_name %></span></p>
          <p class="text-gray-700 mt-4"><%= idea.description %></p>
          <div class="mt-6 flex justify-between items-center">
            <a href="/ideaApp/v1/ideas/update/<%= idea.id %>" class="text-blue-600 font-medium hover:underline">
              Edit
            </a>
            <a href="/ideaApp/v1/ideas/delete/<%= idea.id %>" class="text-red-600 font-medium hover:underline">
              Delete
            </a>
          </div>
        </div>
      <% }); %>
    </div>
  </main>

  <!-- Footer -->
  <footer class="bg-gray-800 text-gray-200 text-center py-4 fixed bottom-0 w-full">
    <p class="text-sm">Idea App &copy; 2024. All rights reserved.</p>
    <p class="text-xs mt-1">Crafted with ❤️ by the Idea App Team</p>
  </footer>
</body>
</html>
